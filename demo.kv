#:kivy 2.0.0

FloatLayout:
    size_hint: (1,1)

    LatexLabel:
        id: sindy
        pos_hint: {"top":1,"x":0.05}
        size_hint:(0.9,1)
        text: r"The SINDy method is a recently [b]developed technique[/b] that leverages sparse regression to identify the governing equations from a given time series (Figure 1). We consider a system with state $\boldsymbol{x}(t)=\left[x_{1}(t), x_{2}(t), \ldots x_{d}(t)\right]^{\top} \in$ $\mathbb{R}^{d}$ governed by the differential equation: $\dot{\boldsymbol{x}}=\boldsymbol{f}(\boldsymbol{x})$ $\square$ $\square$."
        color: (0,0,0,1)
        text_size: root.size
        valign: "top"
        font_size: 20
        font_name: "Roboto"
        line_height: 1.5
        markup: True
